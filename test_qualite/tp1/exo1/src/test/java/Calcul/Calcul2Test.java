package Calcul;

import org.junit.Test;

import static org.junit.Assert.*;

public class Calcul2Test {

    // Faire les test proposer sur: https://sqamate.com/tools/pairwise
    // Jeux de test :
    /*x < -1, x = -1, -1 < x < 1, x = 1, x > 1
    y < 0, y = 0, y > 0
    z < 1, z = 1, z > 1*/

    @Test
    public void calculer() {
    }
}